<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingHorizontal="4dp">

    <!-- dB value display -->
    <TextView
        android:id="@+id/txtDbValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="10sp"
        android:textColor="@color/music_text_primary"
        android:gravity="center"/>

    <!-- Vertical slider container -->
    <FrameLayout
        android:layout_width="36dp"
        android:layout_height="140dp"
        android:layout_marginVertical="4dp"
        android:background="@color/music_surface">

        <!-- The SeekBar will be rotated 270 degrees to become vertical -->
        <SeekBar
            android:id="@+id/seekBarBand"
            android:layout_width="140dp"
            android:layout_height="36dp"
            android:layout_gravity="center"
            android:rotation="270"
            android:max="2400"
            android:progress="1200"
            android:progressTint="?attr/a2uMusicAccent"
            android:thumbTint="?attr/a2uMusicAccent"
            android:splitTrack="false"
            android:thumbOffset="0dp"/>
    </FrameLayout>

    <!-- Frequency label -->
    <TextView
        android:id="@+id/txtFrequency"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1K"
        android:textSize="11sp"
        android:textColor="@color/music_text_secondary"
        android:gravity="center"/>
</LinearLayout>
