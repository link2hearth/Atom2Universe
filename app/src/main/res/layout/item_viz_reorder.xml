<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="4dp"
    android:paddingEnd="16dp"
    android:paddingTop="2dp"
    android:paddingBottom="2dp"
    android:minHeight="48dp">

    <ImageView
        android:id="@+id/drag_handle"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="10dp"
        android:src="@drawable/ic_drag_handle"
        android:scaleType="centerInside"
        app:tint="@color/music_text_secondary"
        android:contentDescription="@string/music_viz_drag_handle" />

    <TextView
        android:id="@+id/viz_position"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/music_text_secondary"
        android:gravity="center" />

    <TextView
        android:id="@+id/viz_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="15sp"
        android:textColor="@color/music_text_primary"
        android:paddingStart="8dp"
        android:paddingEnd="8dp" />

</LinearLayout>
